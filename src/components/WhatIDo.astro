---
const focusAreas = [
	{
		title: "Generative AI",
		description: "Building LLM-powered workflows and intelligent assistants for real use cases."
	},
	{
		title: "Data Science",
		description: "Turning complex datasets into signals that guide product and business decisions."
	},
	{
		title: "Software Engineering",
		description: "Shipping reliable applications with .NET Core, Java, and Python."
	}
];
---

<section id="what-i-do" class="py-20 px-6 lg:py-24">
	<div class="mx-auto max-w-6xl">
		<div class="flex flex-wrap items-end justify-between gap-6">
			<h2 class="font-display text-3xl md:text-4xl">What I do</h2>
			<p class="max-w-xl text-sm text-[color:var(--muted)]">
				A focused mix of AI, data, and software engineering that stays practical.
			</p>
		</div>

		<div class="mt-10 grid gap-6 md:grid-cols-3">
			{focusAreas.map((area) => (
				<article class="rounded-2xl border border-[color:var(--border)] bg-[color:var(--surface)] p-6 transition hover:border-[color:var(--accent)]">
					<h3 class="font-display text-xl text-[color:var(--ink)]">{area.title}</h3>
					<p class="mt-3 text-sm leading-relaxed text-[color:var(--muted)]">
						{area.description}
					</p>
				</article>
			))}
		</div>
	</div>
</section>
